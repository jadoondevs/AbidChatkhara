{
  "name": "@pos/print-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Local ESC/POS print agent for the Abid Chatkhara POS — prints raw bytes to the BIXOLON, bypassing the blank-page Windows driver.",
  "type": "module",
  "bin": { "pos-print-agent": "index.js" },
  "scripts": {
    "start": "node index.js",
    "test": "node --test"
  },
  "engines": { "node": ">=18.0.0" }
}
